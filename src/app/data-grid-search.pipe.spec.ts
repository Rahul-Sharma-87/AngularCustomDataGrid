import { DataGridSearchPipe } from './data-grid-search.pipe';

describe('DataGridSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DataGridSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
