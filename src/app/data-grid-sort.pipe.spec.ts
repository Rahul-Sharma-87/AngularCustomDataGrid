import { DataGridSortPipe } from './data-grid-sort.pipe';

describe('DataGridSortPipe', () => {
  it('create an instance', () => {
    const pipe = new DataGridSortPipe();
    expect(pipe).toBeTruthy();
  });
});
